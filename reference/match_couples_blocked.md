# Match with blocking (multiple problems)

Match with blocking (multiple problems)

## Usage

``` r
match_couples_blocked(
  left,
  right,
  left_ids,
  right_ids,
  block_col,
  vars,
  distance,
  weights,
  scale,
  max_distance,
  calipers,
  method,
  parallel = FALSE
)
```
