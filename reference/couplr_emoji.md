# Get a themed emoji

Get a themed emoji

## Usage

``` r
couplr_emoji(
  type = c("error", "warning", "info", "success", "heart", "broken", "sparkles",
    "search", "chart", "warning_sign", "stop", "check")
)
```
