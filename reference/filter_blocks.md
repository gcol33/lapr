# Filter blocks based on size and balance criteria

Filter blocks based on size and balance criteria

## Usage

``` r
filter_blocks(
  left,
  right,
  min_left,
  min_right,
  drop_imbalanced,
  imbalance_threshold
)
```
