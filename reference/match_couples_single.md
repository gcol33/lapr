# Match without blocking (single problem)

Match without blocking (single problem)

## Usage

``` r
match_couples_single(
  left,
  right,
  left_ids,
  right_ids,
  vars,
  distance,
  weights,
  scale,
  max_distance,
  calipers,
  method,
  check_costs = TRUE
)
```
